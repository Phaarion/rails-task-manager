<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <% if task.completed?%>
    <p> [x]<%= link_to task.title, task_path(task) %></p>
  <% else %>
    <p> [ ]<%= link_to task.title, task_path(task) %></p>
  <% end %>
<% end %>
